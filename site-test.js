const axios = require('axios');
const http = require('http');

async function test() {
  const loginData = {
    email: 'superadmin@gmail.com', // just to get a token, but I don't have password. 
  };
}
