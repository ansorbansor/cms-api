export enum AuthProvidersEnum {
  email = 'email',
  facebook = 'facebook',
  google = 'google',
  twitter = 'twitter',
  apple = 'apple',
}

export enum RoleEnum {
  'admin' = 1,
  'user' = 2,
}

export enum FileTypeEnum {
  image = 0,
  document = 1,
  video = 2,
  audio = 3,
}

export enum RedisKeyEnum {
  user = 'User:',
}
